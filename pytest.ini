[pytest]
markers =
    unit: unit tests (fast, isolated)
    integration: integration tests (DB, models, cache)
    e2e: end-to-end tests (real external services)
addopts = -q
python_files = test_*.py
python_classes = Test*
python_functions = test_*
